name: Test.yml
on:
  [ push ]



jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - name: ls
        run: |
          chmod +x script.sh
          ./script.sh